apiVersion: v1
kind: Service
metadata:
  name: dep-service
  namespace: default
  labels:
    app: dep-service
spec:
  selector:
    app: dep-mag
  type: NodePort
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090
